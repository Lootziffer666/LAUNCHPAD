Added:

• Papa-Modus: a supervised NFC tag / QR code lifts the rules for a set window — no time is spent,
  optionally bound to your home WiFi (auto-ends when leaving)
• Daily base-time allowance so "tomorrow there's new time" actually holds
• App updates the parent can push (checks a release feed; installs silently as device owner)

Fixed:

• Time balance is now written safely under concurrency (no lost earn/spend)
