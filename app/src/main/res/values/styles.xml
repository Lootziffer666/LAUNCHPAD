<resources>

    <!-- HEARTHWORK: DM Sans as the app-wide UI/body font (Nunito applied to display surfaces). -->
    <style name="AppTheme" parent="AppTheme.Base">
        <item name="android:fontFamily">@font/dm_sans</item>
        <item name="fontFamily">@font/dm_sans</item>
    </style>

    <!-- Eltern-Modus: warm brand accent (no green). The dominant surfaces (toolbar, section
         headers, status bar) are re-tinted from the wallpaper at runtime; this just sets a warm
         default so nothing flashes green before that. -->
    <style name="ElternModusTheme" parent="AppTheme">
        <item name="colorPrimary">@color/lp_coral</item>
        <item name="colorAccent">@color/lp_coral</item>
    </style>

    <style name="LauncherTheme" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:fontFamily">@font/dm_sans</item>
        <item name="fontFamily">@font/dm_sans</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowShowWallpaper">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>

        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

</resources>
