<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_holder"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Rules page overlay — starts off-screen left (translationX set in code), INVISIBLE by default -->
    <FrameLayout
        android:id="@+id/rules_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="invisible"
        android:elevation="4dp" />

    <!-- LAUNCHPAD: Rocket splash overlay — shown once per process on cold start. Sits ABOVE everything -->
    <FrameLayout
        android:id="@+id/rocket_splash"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/hw_navy"
        android:elevation="20dp"
        android:visibility="gone">

        <com.airbnb.lottie.LottieAnimationView
            xmlns:app="http://schemas.android.com/apk/res-auto"
            android:id="@+id/rocket_splash_animation"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            app:lottie_rawRes="@raw/rocket_launch"
            app:lottie_autoPlay="false"
            app:lottie_loop="false" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|center_horizontal"
            android:layout_marginBottom="64dp"
            android:text="LAUNCHPAD"
            android:textColor="@color/hw_white"
            android:textSize="22sp"
            android:textStyle="bold"
            android:letterSpacing="0.25" />
    </FrameLayout>

    <include
        android:id="@+id/home_screen_grid"
        layout="@layout/home_screen_grid"
        android:layout_above="@id/launchpad_status_bar"
        tools:ignore="IncludeLayoutParam" />

    <include
        android:id="@+id/all_apps_fragment"
        layout="@layout/all_apps_fragment"
        android:visibility="gone" />

    <include
        android:id="@+id/widgets_fragment"
        layout="@layout/widgets_fragment"
        android:visibility="gone" />

    <View
        android:id="@+id/home_screen_popup_menu_anchor"
        android:layout_width="1dp"
        android:layout_height="1dp"
        android:visibility="gone" />

    <!-- LAUNCHPAD: Jake's status bar (only visible when Kindermodus is ON) -->
    <LinearLayout
        android:id="@+id/launchpad_status_bar"
        android:layout_width="match_parent"
        android:layout_height="42dp"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="56dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="16dp"
        android:background="#CC000000"
        android:visibility="gone">

        <TextView
            android:id="@+id/status_time_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="⏱️"
            android:textSize="16sp"
            android:paddingEnd="8dp" />

        <TextView
            android:id="@+id/status_time_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textColor="#FFFFFF"
            android:textSize="14sp"
            android:textStyle="bold"
            android:text="— Min" />

        <TextView
            android:id="@+id/status_explore_btn"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🌐"
            android:textSize="18sp"
            android:paddingHorizontal="12dp"
            android:paddingVertical="6dp" />

        <TextView
            android:id="@+id/status_tap_hint"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="›"
            android:textColor="#AAFFFFFF"
            android:textSize="18sp"
            android:paddingStart="4dp" />

    </LinearLayout>

</RelativeLayout>
